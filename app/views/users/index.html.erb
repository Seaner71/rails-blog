<h1>Users list</h1>

<ul>
  <% @users.each do |user| %>
  <li>
    <%= link_to user.username, user %>
    <ul>

      <% user.posts.last(3).reverse.each do |post| %>
      <li><%= link_to post.title, post %> </li> 
      <% end %>
    </ul>
    <% end %>
  </li>

</ul>
